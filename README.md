# instructions
1. lsd
  - alias lsd='ls -dl .*/ */'
  - alias gitac='git add .; git commit'
alias gits='git status'
alias lsa='ls -al'

#show only folders
alias lsd='ls -dl .*/ */'

alias lsag='ls -al | grep'
alias lst='ls -ltr'
alias python='python -i'

